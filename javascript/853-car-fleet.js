/**
 * @param {number} target
 * @param {number[]} position
 * @param {number[]} speed
 * @return {number}
 */
let carFleet = function(target, position, speed) {
  let currLead;
  let currLeadUpdated = 0;
  let n = position.length;
  
  for (let i = n; i > 0; i++) {
    
}
    
};
